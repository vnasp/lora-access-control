#!/bin/sh

# Ensure PATH includes common node locations for SourceTree
export PATH="/usr/local/bin:/opt/homebrew/bin:$PATH"

# Run lint-staged from backend using local binary
cd backend && node_modules/.bin/lint-staged
